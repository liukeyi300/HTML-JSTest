/**
 * Created by liukeyi on 2017/1/21.
 */
/*const a = ['1', '2', '3'].map(parseInt);

const b = [typeof null, null instanceof Object];

const c = [[3,2,1].reduceRight(Math.pow)];

const d = 'value';
console.info('Value id ' + (d === 'value') ? 'Something' : 'Nothing');

console.log(a);
console.log(b);
console.log(c);

const e = [1].reduce(Math.pow);
console.log(e);*/

/*
var name = 'World';
(function(){
    var c;
    if(typeof name === 'undefined') {
        var name = "Jack";
        console.info('Goodbye '+ name);
    } else {
        console.info('Hello ' + name);
    }
})();*/

/*
var two = 0.2;
var one = 0.1;
var six = 0.6;
var eight = 0.8;
console.log([two - one == one, eight - six == two]);*/
function sid(arr) {
    arr[0] = arr[2];
}
